# Ask Seoul — 엔지니어링 의사결정 로그

> **목적**: 파이프라인을 구축·운영하며 발생한 문제, 선택지와 트레이드오프, 결정 근거, 해결/실패 과정을 시계열로 기록한다.
> 최상위 목표는 **튼튼하고(견고성) 비용효율적인 데이터 수집 파이프라인**이며, 모든 엔트리는 이 두 축과 연결해 회고한다.
> **관리**: dev 브랜치에서는 untracked로 유지, 기기 간 동기화는 `workspace/mac-sync` 브랜치에 커밋해 수행.

---

## 작성 템플릿

```
### DL-XXX <제목>
- 기간 / 상태: 해결 | 부분해결 | 실패(수용) | 관찰중
- 근거 링크: PR·이슈·커밋·run_id
- 문제: 무엇이, 어떤 증상으로
- 증거/조사: 어떤 데이터로 원인을 특정했나
- 선택지와 트레이드오프: 고려한 대안들과 각각의 비용
- 결정과 근거: 무엇을 왜 골랐나
- 결과: 수치로
- 회고: 배운 것, 면접에서 말할 포인트
```

---

## 인덱스

| ID | 제목 | 상태 |
|---|---|---|
| DL-001 | Trino INSERT 쿼리 크기 한계 → PyIceberg 1커밋 로더 전환 | 해결 |
| DL-002 | KMA API 레이트리밋(429) 대응 — 요청 간격·백오프·체크포인트 | 해결 |
| DL-003 | 1000행 초과 발표분 페이지네이션 미비 → 소급 불가 데이터 결손 | 부분해결(결손 수용) |
| DL-004 | 백필 6런 실패 인시던트 — 대시보드 착시와 알림 신뢰 | 해결 |
| DL-005 | 부분 백필(allow_partial_pages) — 완결성 검증 완화의 트레이드오프 | 해결 |
| DL-006 | Discord 알림 정책 — 실패-only 원칙과 도메인별 코드 중복 | 해결(잔여 리스크 기록) |
| DL-007 | R2 미완료 멀티파트 업로드 잔재 — 저속 네트워크 타임아웃과 쓰기 잔재 | 해결 |
| DL-008 | 네임스페이스 이관(dev_masondev1024→weather_traffic_bronze) — 무복사 RENAME | 해결 |
| DL-009 | traffic transform 주기 전환(5분 asset→hourly cron) — 정확성/신선도 테스트 분리 | 해결 |
| DL-010 | KMA 값 의미 감사 — 표현 체제 발견과 silver 의미 계층 도입 | PR 리뷰 대기(DAG#261·DBT#128) |

---

## 2026-07 기상청(KMA) 단기예보 파이프라인 안정화

파이프라인 구조: KMA getVilageFcst API(서울 80격자 × 일 8회 발표) → R2 raw JSON 적재 → 파싱 → Iceberg 브론즈 적재 → 검증까지 단일 DAG, 3시간 주기 스케줄.

### DL-001 Trino INSERT 쿼리 크기 한계 → PyIceberg 1커밋 로더 전환

- **기간 / 상태**: 2026-07-03 ~ 07-08 / 해결
- **근거 링크**: ASAC-DAG 이슈 #177, PR #178(커밋 580d2e4), 후속 PR #222; hotfix 커밋 3a1ce6c·63bc7a7
- **문제**: 17시(격자당 1,052항목)·20시(1,016항목) 발표분은 한 런에 8만+ 행을 INSERT해야 하는데, SQL 텍스트로 값을 인라인하는 방식이 Trino `QUERY_TEXT_TOO_LARGE` 한계에 걸려 브론즈 적재가 반복 실패. 7/3~7/7 정기 런 20건 실패.
- **증거/조사**: Airflow 실패 로그의 Trino 에러, 실패 런의 base_time 분포가 17·20시에 집중(= 행수와 상관), raw는 정상 적재돼 있음(수집과 적재의 실패 분리 확인).
- **선택지와 트레이드오프**:
  1. **chunk INSERT (hotfix)** — 쿼리를 90만자 이하로 쪼개 순차 실행. 즉시 적용 가능하지만 **chunk 수만큼 Iceberg 커밋(스냅샷+manifest 파일) 증가** → R2 Class A 쓰기 요청 증가, 메타데이터 파일 폭증으로 스냅샷 만료/컴팩션 유지비 상승. 임시로 채택(3a1ce6c).
  2. **PyIceberg transaction 1커밋 (이슈 #177 후보 A)** — SQL을 우회해 Arrow 테이블을 직접 append. `delete(dag_run_id) + append`를 단일 트랜잭션으로 묶어 **런당 커밋 1회** 보장. 비용: Airflow 이미지에 pyiceberg 의존성 추가(리빌드), Trino를 경유하지 않으므로 **동시 커밋 충돌(CommitFailedException)을 직접 처리**해야 함.
  3. **R2 staging Parquet + Trino add_files (후보 B)** — staging 정리 실패 시 raw coverage 오염 리스크, cleanup 실패 처리 경로가 추가로 필요.
- **결정과 근거**: 후보 A. 쿼리 크기와 커밋 폭증을 **동시에** 해소하는 유일한 안이고, staging이 없어 실패 모드가 단순하며, 같은 트랜잭션 안의 선삭제로 재시도 멱등성이 공짜로 따라옴.
- **결과**: 7/8 14:20 KST 이후 정기 런 연속 성공, QUERY_TEXT_TOO_LARGE 재발 0건, 런당 커밋 1회. 파생 문제였던 동시 커밋 충돌은 table refresh + 지수 백오프 재시도로 보강(PR #222). 이슈 #177은 운영 실측 근거와 함께 종료.
- **회고**: "쿼리를 쪼갠다"는 대증요법은 비용 축(커밋 폭증)에서 새 문제를 만들었다. 저장 포맷(Iceberg)의 쓰기 모델까지 내려가서 푸는 것이 견고성·비용 양쪽을 만족했다. hotfix → 장기안 전환 시 이슈에 Acceptance Criteria를 수치로 박아둔 것이 종료 판정을 쉽게 했다.

### DL-002 KMA API 레이트리밋(429) 대응 — 요청 간격·백오프·체크포인트

- **기간 / 상태**: 2026-07-02 ~ 07-09 / 해결
- **근거 링크**: PR #222 (fetch_url `retry_429_backoff_seconds`, `KMA_429_BACKOFF_SECONDS=3600/5400/7200`), landing checkpoint 로직
- **문제**: 80격자 순차 호출 중 429가 뜨면 런이 죽고, 재시도 시 이미 받은 격자를 처음부터 다시 호출해 API 쿼터를 낭비. 실제로 7/2 05시(43객체), 7/3 11시(20객체)처럼 중간에 끊긴 부분 수집 흔적이 남음.
- **선택지와 트레이드오프**:
  - 요청 간 딜레이(4초): 런타임 증가(80격자×4초 ≈ 5.3분+) vs 차단 예방 — 3시간 주기라 런타임 여유 충분, 채택.
  - 429 전용 장기 백오프(1h→1.5h→2h, Retry-After 헤더 우선): 일반 5xx와 분리해 쿼터 리셋 주기에 맞춤.
  - **landing checkpoint**: 격자별 수집 결과를 R2에 체크포인트로 남겨 태스크 재시도 시 이미 받은 페이지를 재사용(`reused_raw_object_count`). 스토리지 쓰기 소폭 증가 vs API 호출 재낭비 방지 — API 쿼터가 더 귀한 자원이므로 채택.
- **결과**: 재시도 시 중복 API 호출 0에 수렴, 429로 인한 런 사망 대신 대기 후 재개.
- **회고**: 외부 API 파이프라인의 비용은 스토리지보다 **호출 쿼터**가 병목. "재시도 = 처음부터"를 없애는 체크포인트가 견고성과 비용을 동시에 잡는 레버였다.

### DL-003 1000행 초과 발표분 페이지네이션 미비 → 소급 불가 데이터 결손

- **기간 / 상태**: 2026-07-01 ~ 07-06(발생), 07-09(범위 확정) / 부분해결 — 결손 수용·문서화
- **근거 링크**: 페이지네이션 구현 커밋 ada8fec(07-06 11:38), R2 실측(발표분별 객체 수 80 vs 160), KMA API 프로브(resultCode=10)
- **문제**: KMA API는 `numOfRows` 최대 1,000인데 17·20시 발표는 격자당 1,016~1,052항목. 페이지네이션 구현(07-06) 이전 수집분은 **모든 격자에서 page-2(마지막 16~52항목)가 아예 수집되지 않음**. 문제를 인지한 시점(백필 실패)에는 이미 원본이 소실된 상태.
- **증거/조사**: R2 객체 수 실측 — 07-06 이전 17/20시 발표 = base당 80객체(격자당 1페이지), 이후 = 160객체(2페이지). KMA API를 직접 프로브해 `resultCode=10 "최근 3일 간의 자료만 제공합니다"` 응답으로 재수집 불가를 확정.
- **선택지와 트레이드오프**: 재수집(불가 확정) / 부분 적재(DL-005) / 결손 인정. — 결손 항목이 각 발표의 **가장 먼 예보시간 꼬리**라서 인접 발표가 같은 시간대를 재예보함 → Silver(최신 발표 우선) 관점 실효 손실 작음이 수용 근거.
- **결과**: 영구 결손 확정 — 7/2 17·20·23시, 7/3 02·05·08시(raw 없음), 7/3 11시의 72격자, 7/2 05시의 38격자, 7/1~7/5 17/20시의 page-2 꼬리.
- **회고**: **수집 시점의 완결성 검증(total_count vs 수신 행수)이 없으면 결손은 조용히 쌓이고, 발견 시점엔 늦다.** 이후 수집 경로에 페이지 완결성 검증을 상시화했다. "API가 과거를 며칠 제공하는가"는 문서가 아니라 **실측**(에러코드 응답)으로 확정하는 것이 정확했다.

### DL-004 백필 6런 실패 인시던트 — 대시보드 착시와 알림 신뢰

- **기간 / 상태**: 2026-07-08(발생) ~ 07-09(규명) / 해결
- **근거 링크**: run_id `manual_recovery_*_20260708T085358Z_01~17`, Airflow REST API 전수 조회
- **문제**: 브론즈만 재적재하는 백필 17런을 돌린 뒤 대시보드에서 "전부 성공"으로 보고 넘어갔는데, Discord로 실패 알림 6건이 계속 도착. 알림 오발송을 의심함.
- **증거/조사**: Airflow API로 런·태스크 상태 전수 조회 → **17런 중 앞 6런이 실제 실패**(`load_kma_bronze` 4트라이 소진). 대시보드 그리드는 최근 런 위주로 보여 실패 6런이 시야 밖에 있었음. 즉 **알림이 옳았고 대시보드 확인 방식이 틀렸다**. 실패 6런의 대상은 전부 DL-003의 페이지네이션 이전 raw — 없는 page-2를 요구하는 완결성 검증에 걸린 구조적(결정론적) 실패라 재시도 4회가 전부 같은 에러였음.
- **부가 발견**: run_id 라벨은 UTC(예: `0800`), 실제 발표시각은 KST(1700)로 9시간 어긋나 초기 분석에 혼선 → 운영 식별자 타임존 통일 필요.
- **결과**: 실패 6런의 원인·범위 확정, DL-005로 연결. "성공처럼 보이는 실패"가 사라짐.
- **회고**: 모니터링의 신뢰 순서를 정립 — **개별 이벤트 알림(정확) > 대시보드 스냅샷(요약·착시 가능)**. 판정이 갈릴 땐 API 원장 데이터로 전수 확인. 또한 재시도는 일시적 실패에만 유효하며, 결정론적 실패에 재시도 4회는 시간 낭비 — 실패 유형 구분이 필요하다.

### DL-005 부분 백필(allow_partial_pages) — 완결성 검증 완화의 트레이드오프

- **기간 / 상태**: 2026-07-09 / 해결
- **근거 링크**: PR #222, run_id `manual_partial_backfill_*_20260709T021245Z`(6런), 단위 테스트 포함 weather 스위트 38 passed
- **문제**: DL-003/004로 확정된 "page-1 raw만 존재하는 5개 발표 슬롯 + 부분 raw 1슬롯"을 브론즈에 적재하려면 페이지네이션 완결성 검증을 통과할 수 없음. 그렇다고 검증을 끄면 신규 수집의 안전망이 사라짐.
- **선택지와 트레이드오프**:
  1. 검증 전역 완화 — 신규 수집의 결손을 다시 조용히 허용하게 됨. 기각.
  2. **conf 옵트인 플래그** — `dag_run.conf.allow_partial_pages=true`일 때만 raise→경고 완화. 정기/리콜렉트 경로는 conf가 없어 strict 유지. 운영자가 명시적으로 켠 백필에서만 동작.
  3. 결손 슬롯 포기 — 슬롯당 95~98%가 살아있는 데이터를 버림. 기각.
- **결정과 근거**: 2번. 추가로 **manifest에 expected_rows(원래 기대치) vs actual_rows(실적)를 그대로 기록**해 부분 적재였다는 사실이 지워지지 않게 함 — 검증을 "끄는" 게 아니라 "판정만 완화하고 가시성은 유지".
- **결과**: TDD(실패 테스트 먼저)로 구현, 6런 전부 성공. 0행이던 5슬롯이 각 80,000행(page-1 전량)으로, 7/3 11시는 raw가 남은 8격자 완결 적재. 부수 발견: 그 8격자 raw는 원 런의 재시도 중복분(20객체)이라 행 중복 16,700행 — raw 계보 보존 원칙에 따라 브론즈에 유지, dedupe는 silver 책임으로 정리.
- **회고**: 데이터 검증 규칙에 예외가 필요할 때의 원칙 — **(1) 옵트인 (2) 범위 최소화(백필 전용) (3) 예외 사용 흔적을 데이터에 남기기**. "완화"와 "무력화"를 가르는 건 이 세 가지다.

### DL-006 Discord 알림 정책 — 실패-only 원칙과 도메인별 코드 중복

- **기간 / 상태**: 2026-07-08(증상) ~ 07-09(해소) / 해결, 잔여 리스크 기록
- **근거 링크**: traffic 적용 커밋 c8f86b7(dev 머지됨), weather 적용 PR #222, 공유 웹훅 도입·revert 이력(#161, 7782bc6)
- **문제**: "성공은 무음, 실패만 즉시 알림"으로 설정했다고 믿었는데 정기 수집 성공 시마다(3시간 주기) 성공 리포트가 발송됨.
- **증거/조사**: 코드 확인 결과 실패-only 정책이 **traffic 도메인에만 적용**(c8f86b7)돼 있었고 weather의 verify 태스크에는 `on_success_callback`이 남아 있었음. PyIceberg 전환으로 런이 전부 성공하기 시작하자 그동안 실패에 묻혀 있던 성공 리포트가 드러난 것.
- **원인 분석**: 알림 코드가 도메인마다 복사-변형된 구조(공유 웹훅 시도 #161도 revert된 이력)라, 정책 변경이 도메인 간에 전파되지 않음.
- **결정과 결과**: weather의 성공 콜백 제거(PR #222). 실패 콜백(manifest 기록 + Discord + RFC9457 problem 문서)은 유지. 알림 노이즈 제거 — 알림이 오면 곧 "조치 필요"라는 신호 대비가 복원됨.
- **회고**: 알림은 신호 대 잡음비가 생명 — 성공 알림이 상시 울리면 실패 알림의 신뢰가 무너진다(DL-004에서 실제로 알림을 의심하게 됨). **잔여 리스크**: 알림 정책이 여전히 도메인별 복붙이라 culture/population 등에서 같은 불일치가 재발 가능 → 공통 콜백 모듈로의 통합이 후속 과제.

### DL-007 R2 미완료 멀티파트 업로드 잔재 — 저속 네트워크 타임아웃과 쓰기 잔재

- **기간 / 상태**: 2026-07-09 05:35~05:45 KST 발생 / 해결 — MPU 3건 abort, 타임아웃 10초 완화 + 적재 시 잔류 MPU 자동 청소 도입(PR #240 머지), 고아 파일 7건(122,760B) 미참조 검증 후 삭제까지 당일 완료
- **근거 링크**: run_id `scheduled__2026-07-08T20:20:00+00:00`(0709 05시 발표분), `load_kma_bronze` try 1~3 로그, ASAC-DAG PR #240
- **문제**: seoul-dev 버킷 대시보드에 `__r2_data_catalog/.../bronze_kma_vilage_fcst-*/` 아래 "Ongoing Multipart Upload" 3건이 노출됨 (data parquet 1, manifest m0.avro 1, manifest-list snap-*.avro 1 — 전부 0파트·0바이트).
- **증거/조사**: ① `list_multipart_uploads`로 전체 키·시작시각 확보(20:35:46/20:39:15/20:44:12 UTC) → ② Airflow API로 같은 시간창의 태스크 전수 조회 → `load_kma_bronze`가 try 4에서야 성공한 런의 **실패 try 1·2·3과 1:1 대응** 확인 → ③ 각 try 로그에서 동일한 `OSError: UploadPart ... NETWORK_CONNECTION curlCode 28, "Less than 1 bytes/sec transferred the last 3 seconds"`. 같은 런의 `land_kma_raw`도 try 2까지 감 → 해당 10~15분간 로컬 네트워크 저하 창으로 판단. 같은 창의 traffic 런들은 전부 try 1 성공 → 돌발정보 도메인 무관(초기 추정과 달리 KMA 단독).
- **원인 구조**: pyarrow S3FileSystem은 파일을 열 때 멀티파트 업로드를 생성하고, UploadPart가 AWS C++ SDK 기본 저속 타임아웃(3초간 1B/s 미만이면 curl 28)에 걸리면 예외로 죽는데, 이때 AbortMultipartUpload가 호출되지 않아 MPU가 버킷에 잔류한다. try마다 커밋의 다른 단계(데이터 파일 / manifest / manifest-list)에서 끊겨 파일 종류가 제각각.
- **결과/영향**: 잔류 MPU 3건은 0바이트라 저장비용 없음(UI 노출뿐). 실패 try들이 남긴 **완료-업로드 고아 파일 7건(~160KB)**은 어느 커밋에도 미참조 — 실패 스냅샷 ID(4482546674805048140, 8569066137851075571, 5660233266030080415)가 `$snapshots`에 없음을 확인. 데이터 정합성은 무결: REST 카탈로그 커밋은 원자적이라 실패 try는 테이블에 보이지 않았고, try 4가 delete+append 멱등 로직으로 전량 적재(스냅샷 6864792066208642488, 3147671977257331440).
- **대응/예방 선택지와 결정**: ① 잔류 MPU abort(즉시, 무위험 — PyIceberg는 MPU를 재개하지 않음) → 07-09 완료(3건 abort, 잔류 0건 확인). 발생 자체는 S3 쓰기의 구조적 특성이라 못 막으므로, 재발분은 적재 시 자가 청소로 전환 — `_pyiceberg_table()`이 테이블 prefix의 1시간 이상 된 미완료 업로드를 best-effort abort(동시 런의 살아있는 쓰기는 연령 임계로 보호, 청소 실패는 적재를 막지 않음). "raw/ 하위에 보관" 안은 기각 — MPU는 객체가 아니라 0바이트 업로드 세션이라 옮길 실체가 없고, raw/는 원본 페이로드 계보 전용이라 운영 잔재를 섞으면 계약이 흐려짐. ② R2 라이프사이클 룰 자동 abort → 미채택(수동 관리 선택). ③ `s3.request-timeout` 완화 → **10초로 적용(PR #240)**. 5초 대비 근거: 이번 정체는 3초 컷에 3연속 걸릴 만큼 반복적이라 5초로도 실패 가능성이 컸고, 타임아웃 증가 비용(죽은 연결에서 몇 초 대기)은 재시도 1회 비용(raw 160객체 재다운로드 + 파일 재작성 + 잔재 추가)보다 훨씬 작다. 환경변수 `R2_S3_REQUEST_TIMEOUT`로 오버라이드 가능. ④ 고아 파일 정리 → 07-09 완료: 실패 스냅샷 ID 3개가 테이블 전체 이력(스냅샷 2,340개)에 없음을 확인한 뒤 7건(122,760B) 삭제, 삭제 후 당일 파티션 348,400행 정상 조회로 무결성 검증.
- **회고**: Airflow 재시도는 설계대로 회복시켰지만(try 4 성공), "성공했으니 끝"이 아니라 **실패한 시도의 쓰기 잔재가 스토리지에 남는다**. 원자적 커밋의 이면 — 커밋 밖에서 죽은 파일은 아무도 참조하지 않지만, 아무도 지워주지도 않는다. 견고성 축은 재시도가, 비용 축은 라이프사이클 룰·청소가 맡아야 누수가 없다.

### DL-008 네임스페이스 이관 — dev_masondev1024 → weather_traffic_bronze (무복사 RENAME)

- **기간 / 상태**: 2026-07-09 21:20~22:00 KST(이관) + 07-10 오전(물리 정리) / 해결
- **근거 링크**: `iceberg_dev.weather_traffic_bronze`(bronze 4종), 구 네임스페이스 물리 prefix `__r2_data_catalog/019f0c64-*/`
- **문제**: 개인 닉네임 스키마를 팀 컨벤션(도메인 네임스페이스)에 맞춰 개명 필요. 사전 수동 시도(복사→삭제→원복)로 테이블 물리 위치가 바뀌고 스냅샷 이력이 리셋되고 v0 스모크 silver/gold가 재생성된 상태. 5분 주기 적재가 살아 있어 이관 중 행 유실 위험.
- **증거/조사**: ① **원복본 완전성 검증** — 드롭된 구 테이블의 R2 잔존 metadata.json에서 최종 total-records를 읽어 현재본과 대조. kma: 구 4,764,058 + 165,440(17시 발표 84,160 + 20시 81,280, **정확 일치**) = 현재 4,929,498 ✓. ② 파티셔닝은 원본도 NONE — 기억(문서)보다 메타데이터(원장). ③ 이름 충돌 전수조사: weather/traffic(신축 dbt 작업장, live silver가 census상 '세대'로 오인될 뻔), ops_smoke/dev_local/팀원 스키마 — 충돌 없음.
- **선택지와 트레이드오프**: A) CTAS 복사 — 490만 행 재작성(R2 Class A 쓰기·홈 회선 시간·실패 리스크), 이력 리셋. B) `ALTER TABLE ... RENAME` 교차 스키마 이동 — 메타데이터 연산(즉시·무비용·이력 보존), 단 R2 Data Catalog의 rename 지원 불확실 → **최소 테이블(audit 1,688행)로 선실험**.
- **결정과 결과**: B 채택(실험 성공). 절차: 쓰기 DAG 7종 일시정지(실행 TI 0 확인) → RENAME ×4 → 행수 전항목 일치 → 스모크 3종 DROP → 빈 스키마 DROP → `.env` ASK_SEOUL_SCHEMA 교체(+병합돼 깨져 있던 16행 수선) → compose 재생성 → 컨테이너 env 실측 → 재개. **재개 직후 traffic 2런이 새 스키마에 +14/+14 적재(E2E 실증), 유실 0, 정지 창 ~35분.**
- **함정/부수 발견**: ① 25시간 가동된 구 Trino 컨테이너 시계가 **+2h24m 드리프트**(WSL2 절전 후유증) — 쿼리ID가 미래 시각으로 찍혀 조사 혼선, 컨테이너 재생성으로 해소(committed_at은 카탈로그 서버 시각이라 신뢰 가능). ② `.env`는 gitignored — **맥의 .env는 여전히 구 스키마를 가리킴**(맥 스택 기동 시 dev_masondev1024 부활 위험). ③ dags 레포 테스트에 구 스키마명 하드코딩 잔존(후속 PR). ④ DROP TABLE/SCHEMA는 R2 물리 파일을 지우지 않음 — 07-10 정리 실행: **50,925 객체 삭제(에러 0)**, 구 bronze 4종은 각 최종 metadata.json만 **복원 앵커로 보존**(필요시 register_table로 전체 복원 가능), live 4,109 객체·weather/traffic 네임스페이스는 위치 기반 제외로 보호. 정리 후 kma 520만 행 풀스캔 통과로 무손상 검증. 테스트 픽스처의 구 스키마명 하드코딩도 PR #252로 제거(70 passed).
- **회고**: 카탈로그가 rename을 지원하면 이관은 복사가 아니라 **포인터 이동**이다 — 대용량일수록 먼저 rename 가능성을 실험하라. 완전성 검증의 기준은 살아있는 테이블이 아니라 **드롭된 원본의 마지막 메타데이터**였다(원장 원칙의 확장). 그리고 타임스탬프가 안 맞으면 데이터보다 **시계**를 먼저 의심하라.

### DL-009 traffic transform 주기 전환(5분 asset → hourly cron) — 테스트가 결합한 '정확성/신선도' 분리

- **기간 / 상태**: 2026-07-10 11:38~13:15 KST / 해결
- **근거 링크**: ASAC-DBT PR #106, `.env` `ASK_SEOUL_TRAFFIC_TRANSFORM_DAG_SCHEDULE="10 * * * *"`, 실패 런 `scheduled__2026-07-10T03:10:00+00:00`
- **문제**: transform DAG가 `schedule=[Asset(bronze)]`로 bronze의 5분 주기를 그대로 물려받아 **dbt 전체 파이프라인(deps→freshness→seed→run→test)이 하루 288회** 실행 — 런마다 Trino 쓰기·Iceberg 커밋·tmp 테이블 잔해(R2 Class A)가 쌓임. 돌발정보의 신선도 요구는 시간 단위면 충분.
- **선택지와 트레이드오프**: ① 유지(비용 지속) ② 하루 1회(당일 사고가 다음날 반영 — 제품 가치 하락) ③ **시간당 1회(채택)** — 288→24회/일. weather는 asset 트리거가 이미 발표 주기(8회/일)에 정확히 정렬돼 있어 유지 — **도메인별 차등이 핵심**(일괄 정책 아님).
- **과정/증거**: 코드에 준비돼 있던 env 탈출구를 사용해 코드 무변경 전환. 컨테이너 재생성은 "실행 중 태스크 0" 창을 감시해 원자적으로 수행(진행 중 런 보호 — 태스크 단위가 아니라 **런 단위** 종결을 기다림). 전환 후 asset 미발화 실증. 그러나 **첫 cron 런(12:10 KST)이 FAIL 22** — silver는 12:05 배치까지 읽고 빌드됐는데 1분 뒤 테스트는 갓 커밋된 12:10 배치와 비교. cron `:10`이 bronze `:10` 런과 동시 출발해 생긴 경합으로, asset 시절엔 "bronze 완료 직후 실행"이라 구조적으로 없던 문제. 재시도도 같은 22행 실패 → 레이스가 아니라 결정적 불일치임을 확인.
- **결정과 근거**: 스케줄을 되돌리는 대신 **테스트를 수정**. `assert_silver_traffic_latest_publishable_record`가 정확성(silver가 처리 시점까지의 최신본을 골랐는가)과 신선도(bronze 절대 최신과 일치하는가)를 한 쿼리에 결합하고 있었고, 후자는 hourly 전환으로 **의도적으로 깨지는 계약**(최대 1시간 지연 수용). bronze 비교 대상에 silver watermark(max collected_at) 상한을 추가해 정확성만 검증 — 신선도는 `dbt_source_freshness`가 이미 별도 감시.
- **결과**: 실패 조건 재현 상태에서 단독 테스트 PASS=1 → 13:10 KST 정기 런 성공. transform 부하 1/12로 감소.
- **회고**: **스케줄은 데이터 계약의 일부다** — 주기를 바꾸면 "최신"의 정의가 바뀌고, 그 정의에 암묵적으로 기대던 테스트가 드러난다. 첫 런의 실패는 버그가 아니라 결합을 드러낸 신호였다. 진단 경로: BashOperator의 `exit code 1` 포장 예외에는 정보가 없다 — dbt 태스크 실패는 dbt.log(FAIL 노드)가 원장이다.

### DL-010 KMA 값 의미 감사 — 표현 체제 발견과 silver 의미 계층 도입

- **기간 / 상태**: 2026-07-10 감사 → 이슈 분할(ASAC-DAG#260, ASAC-DBT#113) → 구현 / PR 리뷰 대기(DAG PR#261, DBT PR#128)
- **문제**: silver 의 값 해석이 `try_cast(fcst_value as double)` 단일 캐스트. 실측: PCP/SNO 의 **78.2%가 num NULL**이고 그 NULL 안에 '강수없음'(명시적 없음)과 '2.0mm'(정량 — 단위 때문에 cast 실패 89,926행)가 무구분 혼재. 같은 의미가 근구간 '강수없음'→NULL / 원구간 '0'→0.0 으로 이중 인코딩.
- **핵심 발견(실데이터)**: PCP 표현이 **리드타임 ~50h 경계로 두 체제** — 근구간(1~70h) '강수없음'/'1mm 미만'/'X.Ymm', 원구간(50~103h) '0'/맨몸 숫자('1','0.2'). 코드가 아니라 표현 분류 × lead 분포 쿼리로 발견. TMN=0600·TMX=1500 전용(sparse), WAV 전량 '0', PTY {0,1,4}·SKY {1,3,4}, unknown category 0종.
- **결정**: ① Bronze 는 이미 교과서적(원형 varchar·무필터·lineage) — 무변경, page_no 일급 컬럼만 추가(#260). ② Silver 에 additive 의미 계층: `kma_value_semantics` 매크로 → value_representation/value_num(정량만)/lower·upper bound('이상'의 무상한 보존)/qualitative_code + forecast_lead_hours. ③ 원구간 맨몸값은 공식 매핑 검증 전까지 **bare_numeric 으로 정직 분류** — is_extended 불리언 미도입(50h는 실측 상관일 뿐, 사실만 제공). ④ drift 는 차단이 아니라 감지: unparseable=0 알람 + category/PTY·SKY 도메인 warn + PTY↔PCP 모순 warn.
- **결과**: silver 재빌드 후 테스트 10/10 PASS, 정량 72,911행 복원(전엔 NULL), 범위 44,014행 bounds 파싱, unparseable 0. 기존 fcst_value_num 호환 유지(additive).
- **회고**: "값이 숫자다"는 스키마 가정은 소스의 **표현 계약**을 조사해야 검증된다 — 감사의 순서(코드 의도 → git 이력 → 실데이터 분포 → 리드타임 교차)가 try_cast 뒤에 숨은 의미 손실을 드러냈다. 모르는 것(연장 맨몸값의 공식 의미)은 추정하지 않고 이름에 드러냈다(bare_numeric). 이슈→브랜치→PR 분할로 dags/dbt 변경의 리뷰 단위를 분리.

---

## 종합 회고 — 이번 스프린트에서 자리 잡은 원칙

1. **증거 사슬로 디버깅한다**: 코드 → git 이력(언제 어떤 로직이 배포됐나) → Airflow API(실제 런 상태 전수) → R2 실측(원본이 실제로 있나) → Trino(적재 결과 수치) → 외부 API 프로브(소스가 뭘 주나). 각 단계가 가설을 하나씩 확정/기각했다.
2. **멱등성은 트랜잭션 경계에서 산다**: 같은 dag_run_id 선삭제 + append를 한 커밋에 묶으면 재시도가 안전해진다.
3. **비용효율은 세 곳에서 결정됐다**: Iceberg 커밋 수(메타데이터·R2 쓰기), API 호출 쿼터(체크포인트 재사용), 알림 노이즈(운영자 주의력도 비용).
4. **검증의 예외는 옵트인 + 최소 범위 + 흔적 남기기**.
5. **대시보드보다 원장(API·테이블)을 믿는다**.

<!-- 새 엔트리는 템플릿을 복사해 해당 시기 섹션 아래에 DL-순번으로 추가 -->
